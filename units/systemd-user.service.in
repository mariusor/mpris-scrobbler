[Unit]
Description = daemon to submit music retrieved from MPRIS DBus interface to libre.fm and compatible services
Requires = dbus.socket

[Service]
Type = simple
ExecStart = DESTDIR`'INSTALL_PREFIX`'BINDIR/DAEMONNAME -vv
ExecReload = /bin/kill -HUP $MAINPID

